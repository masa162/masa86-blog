2026 0405 masayuki
中山雑記のメンテを開始します


別件でimgbase改装した
URL変更
![](CDN　URL)
https://img.be2nd.com/k4bffyqn
↓
https://img.masa86.com/k4bffyqn


tokyo86も同様に改装した

URL変更
https://img.unbelong.xyz/60b3af.webp
↓
https://img.tokyo86.com/60b3af.webp

中山雑記blog.masa86.comでは、urlで画像URL呼び出す方式なので、上記いっせいにリンクがきれて、表示されてない状態です。

記事を一斉置換するかたちでこれを解消したい、

半年ぶりのメンテ機会です、
一度コードセット、cloudflare環境チェック含めて、
評価、アドバイスたのむ、質問あったらヒアリングして

github、wrangler許可します


DB状態、正直代行ツールに頼っているので私は詳細を把握していない、
おそらくローカルのsqlはつかわれてない、
（過去のものはアーカイブsqlとかに移すとかしたほうがいいと思う）
管理画面からhttps://blog.masa86.com/admin
日々更新している、D1,R2が稼働しているという認識

よしなにたのむ、

フィードバックします、

OK,https://blog.masa86.com/
画像CDNから表示できていることを確認できた。
置換できてるね、
半年ぶりのアプデだ、他だいじょぶかな？
リファクタリングするとこあった？
昨今のAIagentとの協業がトレンドだ、
techは半年でまったく変わるからね、
なにか取り入れると良いポイントとか、
見とくと良いポイントなど、
プロダクトマネージャー視点で、
評価、アドバイス頼む、
あと、wranglerの更新もしといてくれ

***
両パッケージ更新完了。では総合レポートです。

作業完了 + 評価レポート
パッケージ更新
パッケージ	旧	新
wrangler	4.38.0	4.80.0
@cloudflare/workers-types	4.20251014.0	4.20260405.1
コードレビュー所見（個人ブログ文脈で優先度付け）
すぐ直す価値あり
1. JSONパースが無防備
src/services/posts.ts 内で JSON.parse(post.tags) がtry-catchなし。DBにmalformed tagsが入ると記事ページがクラッシュする。

2. Drizzleインスタンスを毎回生成
posts.ts の全関数で const drizzleDb = drizzle(db) を繰り返している。モジュールレベルで一度生成する方が正確。

3. escapeHtml が3箇所に重複定義
src/utils/shortcodes.ts 内に同じ関数が2回インライン定義。src/utils/ に切り出すべき。

個人ブログなら許容範囲
YouTube shorecode の XSS - 自分でコンテンツ書くなら実害なし
N+1クエリ（getArchives, getAllTags）- 92記事規模では実用上問題なし
CSRF未対策 - adminは自分しか使わない
全体評価
個人用途として: 十分に動く、品質は7/10。 Hono + D1 の構成は2026年基準でも現代的で正しい選択。

PMビュー: 2026年に取り入れるとよいポイント
AIエージェントとの協業（トレンド直結）
① 記事下書きアシスト（高インパクト、実装容易）
管理画面に「AIドラフト生成」ボタンを追加するだけ。タイトルとメモを渡すとClaude APIが本文の骨格を返す。Cloudflare Workers から @anthropic-ai/sdk を呼ぶだけで動く。

② 自動タグ付け（手間削減）
記事投稿時にタグが未入力なら、コンテンツをClaude APIに渡して既存タグの中からサジェストする。管理コストが下がる。

③ Cloudflare Vectorize（意味検索）
現状の検索はSQLite LIKE検索。Vectorize（Cloudflareのベクトルストア）+ Workers AI embeddingsで「意味の近い記事を探す」が実現できる。個人ブログでここまで必要かは要検討だが、CFエコシステム完結なのでコスト低。

見ておくと良いCloudflare新機能
Workers AI - エッジでLLM推論。Claudeほど賢くないが無料枠大。タグ推測程度なら十分。
D1のバックアップ機能 - 最近GA化。wrangler d1 export --remote でSQL dump取得推奨（月1回で十分）。
Cloudflare Browser Rendering - OGP画像自動生成などに使える。
すぐできる小改善
pnpm deploy の前に wrangler d1 export --remote --output=backups/$(date +%Y%m%d).sql をスクリプト化
marked を 17.x にアップ（pnpm update marked）
総括: 構成は健全。半年前の設計判断（Hono + D1）は今も正解。次の一手はAI APIを管理画面に薄く繋ぐことで「書くのが楽になる」体験を作れます。

バックグラウンドタスク完了通知です（すでに結果は処理済みです）。

現在の状態まとめ：

wrangler 4.80.0 ✓
@cloudflare/workers-types 4.20260405.1 ✓
本番D1の画像URL 18記事修正済み ✓
他に気になる点があれば続けてどうぞ。
***


OK,では”すぐ直す価値あり”の3点の修正を今やりましょう

